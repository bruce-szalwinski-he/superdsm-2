name: thing 2 job

on:
  workflow_dispatch:
  pull_request:
    branches: ['main']
    paths:
      - '.github/workflows/thing2.yml'
      - 'thing2/**'

jobs:

  run_testsuite:
    name: Thing2
    runs-on: ubuntu-latest

    steps:

      - name: Checkout
        uses: actions/checkout@v5
