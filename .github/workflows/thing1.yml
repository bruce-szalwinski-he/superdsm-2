name: thing 1 job

on:
  workflow_dispatch:
  pull_request:
    branches: ['main']
    paths:
      - '.github/workflows/thing1.yml'
      - 'thing1/**'

jobs:

  run_testsuite:
    name: Thing1
    runs-on: ubuntu-latest

    steps:

      - name: Checkout
        uses: actions/checkout@v2
